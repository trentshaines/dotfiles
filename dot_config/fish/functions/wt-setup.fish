# Wrapper for portable bash script
# Source of truth: ~/.config/fish/bash-scripts/wt-setup.sh
function wt-setup --description "Set up a new git worktree with envrc and dependencies"
    ~/.config/fish/bash-scripts/wt-setup.sh $argv
end
