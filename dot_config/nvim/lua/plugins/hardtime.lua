return {
  "m4xshen/hardtime.nvim",
  lazy = false,
  dependencies = { "MunifTanjim/nui.nvim" },
  opts = {
    -- Warn/hint instead of blocking keys ("block" is the default)
    restriction_mode = "hint",
  },
}
