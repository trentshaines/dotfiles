- name: Install Homebrew packages on macOS
  hosts: localhost
  become: no
  tasks:
    - name: Check if Homebrew is installed
      command: brew --version
      register: brew_check
      ignore_errors: yes

    - name: Install Homebrew
      shell: |
        /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
      when: brew_check.rc != 0

    - name: Add brew taps
      homebrew_tap:
        name:
          - joshmedeski/sesh
          - felixkratz/formulae
          - kyu08/tap
          - nikitabobko/tap
          - getsentry/tools
        state: present

    - name: Install brew formulae
      homebrew:
        name:
          # Core (must be available before mise)
          - git
          - tmux
          - fish
          - chezmoi
          - mise

          # Database tools
          - postgresql@17
          - pgcli

          # Utilities
          - htop
          - btop
          - tree-sitter
          - sesh
          - tmuxinator
          - borders
          - fzf-bookmark-opener
          - tree
          - coreutils

          # Security
          - gnupg
          - pass
          - pass-otp
          - pinentry-mac

          # Docker
          - docker

          # Beandex server infrastructure
          - cloudflared
          - crane
          - sentry-cli

        state: present

    - name: Install brew casks
      homebrew_cask:
        name:
          - raycast
          - obsidian
          - alacritty
          - font-hack-nerd-font
          - orbstack
          - tailscale
          - aerospace
          - karabiner-elements
          - espanso
        state: present
      ignore_errors: yes

    # After setup, some apps require manual permission grants in System Settings:
    # - Karabiner-Elements: approve System Extension + Input Monitoring
    # - Espanso: grant Accessibility access, then run: espanso service start
    # - AeroSpace: grant Accessibility access
    # After installing obsidian cask: enable CLI in Settings > General > Advanced > Command line interface

    - name: Install Oh My Zsh
      shell: |
        if [ ! -d "$HOME/.oh-my-zsh" ]; then
          sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" "" --unattended
        fi
      args:
        creates: "{{ ansible_facts['env']['HOME'] }}/.oh-my-zsh"

    - name: Add fish to /etc/shells
      become: yes
      lineinfile:
        path: /etc/shells
        line: /opt/homebrew/bin/fish
        state: present
      when: ansible_facts['os_family'] == 'Darwin'

    - name: Set fish as default shell
      become: yes
      user:
        name: "{{ ansible_facts['env']['USER'] }}"
        shell: /opt/homebrew/bin/fish
      when: ansible_facts['os_family'] == 'Darwin'

    - name: Install Fisher (fish plugin manager)
      shell: |
        /opt/homebrew/bin/fish -c "curl -sL https://raw.githubusercontent.com/jorgebucaran/fisher/main/functions/fisher.fish | source && fisher install jorgebucaran/fisher"
      args:
        creates: "{{ ansible_facts['env']['HOME'] }}/.config/fish/functions/fisher.fish"

    - name: Install mise-managed tools
      shell: |
        eval "$(/opt/homebrew/bin/brew shellenv)"
        mise install --yes
      args:
        creates: "{{ ansible_facts['env']['HOME'] }}/.local/share/mise/installs"

    - name: Install TPM (Tmux Plugin Manager)
      git:
        repo: https://github.com/tmux-plugins/tpm
        dest: "{{ ansible_facts['env']['HOME'] }}/.tmux/plugins/tpm"
        update: no

    - name: Install Claude Code via native installer
      shell: |
        curl -fsSL https://raw.githubusercontent.com/anthropics/claude-code/main/install.sh | sh
      args:
        creates: "{{ ansible_facts['env']['HOME'] }}/.local/bin/claude"

    - name: Install global Python tools via uv
      shell: |
        eval "$(/opt/homebrew/bin/brew shellenv)"
        eval "$(mise activate bash)"
        uv tool install autowt
        uv tool install python-lsp-server
      args:
        creates: "{{ ansible_facts['env']['HOME'] }}/.local/bin/autowt"

    - name: Install global npm packages
      shell: |
        eval "$(/opt/homebrew/bin/brew shellenv)"
        eval "$(mise activate bash)"
        npm install -g {{ item }}
      loop:
        - pyright
        - typescript-language-server
        - typescript
        - tree-sitter-cli
        - "@openai/codex"
